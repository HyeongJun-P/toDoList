import React from 'react';
import DoLitem from './DoLitem';
import './DoList.scss';

const DoList = ({todos}) => {
    return (
        <div className="DoList">
        </div>
    );
}

export default DoList;